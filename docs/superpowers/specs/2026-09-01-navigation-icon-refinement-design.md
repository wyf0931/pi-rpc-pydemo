# Navigation Icon Refinement Design

Replace only the two requested Lucide icon names: Chat files uses
`folder-input`; sidebar Library uses `library`. Event handlers, labels, routing,
and dynamic Lucide rendering remain unchanged.
