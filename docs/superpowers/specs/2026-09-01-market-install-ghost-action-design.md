# Marketplace Install Ghost Action Design

Marketplace search-result install actions retain their icon-only plus control,
loading state, disabled state, and accessible label, but use daisyUI's
`btn-ghost` style to reduce visual competition with the result content.
