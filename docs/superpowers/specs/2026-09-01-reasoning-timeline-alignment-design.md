# Reasoning Timeline Alignment Design

Align the custom compact DaisyUI timeline by removing the top margins that push
reasoning/process content down and applying a small, explicit marker offset to
match the visible content start. Preserve the timeline grid, vertical rule,
icons, row spacing, and light/dark theme behavior.
