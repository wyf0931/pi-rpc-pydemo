# Web Fetch Link Icon Clip Design

Each fetched-page link separates its truncatable title text from the external
link icon. The title alone clips with an ellipsis; the icon is a non-shrinking
flex child and remains fully visible.
