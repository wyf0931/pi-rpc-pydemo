# Chat Markdown Typography Design

Only final assistant-answer text will render inside the existing Tailwind
Typography `prose` scope. In dark theme it also receives `prose-invert`.
Reasoning and tool timeline content remain on their compact custom styles, so
heading, table, list, and horizontal-rule presentation improves without
changing timeline density or tool rendering.
