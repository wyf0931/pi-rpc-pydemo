# New Chat Welcome Design

The welcome state gains a centered OMA logo, the existing headline, and a
regular-size supporting line. The composer keeps its dropdown overflow but
uses daisyUI `--radius-box` corners on its visible textarea and footer surfaces
so all four card corners render consistently.
