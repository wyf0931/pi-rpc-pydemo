# Agent Filter Picker Design

Library and Autopilots replace their native agent selects with controlled,
opaque avatar pickers using the same visual language as New Chat. Each picker
shows the selected avatar and name, renders avatar/name options, closes on
selection or outside click, and reloads its existing filtered data source.
