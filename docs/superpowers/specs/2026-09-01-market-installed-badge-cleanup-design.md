# Market Installed Badge Cleanup Design

The Market Skills and Extensions catalog already lists installed resources, so
their `Installed` badges add no state information. Remove those two badges
while retaining the MCP Servers `Available` badge, which has distinct meaning.
